<%= form_for(@research_item) do |f| %>
  <% if @research_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@research_item.errors.count, "error") %> prohibited this research_item from being saved:</h2>

      <ul>
      <% @research_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
