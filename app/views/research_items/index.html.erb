<h1>Research Items</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @research_items.each do |research_item| %>
  <tr>
    <td><%= research_item.name %></td>
    <td><%= link_to 'Show', research_item %></td>
    <td><%= link_to 'Edit', edit_research_item_path(research_item) %></td>
    <td><%= link_to 'Destroy', research_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Research item', new_research_item_path %>
